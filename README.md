# daohang1
研究报告：
一大题：
    1//这个升序排序其实很基础，只需要用for循环实现冒泡排序。先写一个小的for循环，遍历所有的数并，判断前后两个数的大小，目的是将最大的数排到最后。
              //然后再把这个for循环套进一个大for循环中，重复上面的过程，直到将所有的数排好。
              //需要注意的是for循环i < 几，小的for循环遍历次数应该少于总次数 - 大循环走过的次数，不然会操作到超过数组长度的空间，导致报错。
    2//这个也很简单，定义结构体不必多说了，主要是要将输入函数参数设置为指针，做到地址传递。考核要求从键盘输入，需要用cin将输入给予指针指向的数据。然后在输出函数中解引用，在主程序里调用。
    3//这一题主要在类的定义，在私有区域定义颜色和车牌。在公共区域定义函数。main函数中只需调用两个类的构造函数即可。
二大题：
    //这道题属实难到我了，去网上找ubuntu的资源还需要注册一个账户，关键它不支持国内邮箱，而我正好把我谷歌邮箱密码忘记了。最后也是别人发给我的软件。
    //还有ros2的安装，我可能我分配的虚拟内存不够，一开始安装过程中老卡死，后来找的淘宝才解决。
    //不过题目本身很简单，只是对Ubuntu的基础运用，我对这个系统的运行逻辑不太清楚，但我也知道先创建工作空间（命名为RM），将现成的src文件拖进去，编译一下并激活环境，然后打开package和CMake文件寻找一下包和可执行文件的名称，最后ros2 run运行即可。
